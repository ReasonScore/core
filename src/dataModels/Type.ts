/**
 * Different types of items/ data that can be a target of an edge
 * Can we supported or discupte by claims
 */
export enum Type {
    claim = "claim",
    claimEdge = "claimEdge",
    score = "score",
    view = "view",
    viewEdge = "viewEdge",
}